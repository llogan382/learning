# Concepts to study up on. 

- memoizations
- hoisting
- call stack
- call, apply, bind
- decorators
- higher order functions. 
- generators
- Map vs Object
- inheritance in JS Classes
- difference of let, var, const
- The event loop: execution order of code. 
- Weakmap, Weakset
- Symbol
- closure
- deep copies, shallow copies
- concurrency. It is managed witht he event loop, the non-blocking I/O, callbacks, promises and async/await
- event bubbling
- iterators
- BigInt vs Number
- tagged template literals
- `super()` when using classes. 



- `this`
- `new` . Makes the Object for us. Returns it automatically. Refers to it on `this`. 